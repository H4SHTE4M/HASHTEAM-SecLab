#!/bin/sh
# 关卡验证入口：实际评分逻辑在 SUID 签名检查器 htcheck 中。
# htcheck 以调用者身份运行当前关卡的 check.sh，只有退出码为 0 的结果
# 才会用 root-only 会话密钥签名；学生绕过本包装器直接调用 htcheck
# 同样只能得到「真实通过」的签名，无法伪造。
exec "${HASHTEAM_HTCHECK:-/usr/local/bin/htcheck}" run "$@"
